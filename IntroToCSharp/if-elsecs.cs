﻿/*//inputs are used by calling the Console.ReadLine() method
//methods in this case will be different from python because we will use the dot notation ( . ) to call our methods/function
//so instead of print() we will call Console.ReadLine()
//also Console.ReadLine() does not have a prompting feature

Console.WriteLine("Please enter your name: ");
//create a variable with the type we are looking for and store it
String name = Console.ReadLine();
//to concatonate or combine strings, the simplist method is to use the + symbol
Console.WriteLine("Hello " + name);

//parsing - converting from a string or unknown value to a int or float or arithmetic value
//adding two numbers together the user gives us
Console.WriteLine("Please give me a number to add: ");
//you can parse by saying datatype.Parse()
int number1 = int.Parse(Console.ReadLine());
Console.WriteLine("Please give me a second number: ");
int number2 = int.Parse(Console.ReadLine());
int answer = number1 + number2;
Console.WriteLine("Your answer is " + answer);
*/
